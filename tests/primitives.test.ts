// createSignal / createMemo / createEffect were removed in 1.4.0 (they
// were one-line forwards for signal / derived / effect). This test file
// is kept as an empty placeholder to avoid a delete in this change set.
import { describe, it } from "vitest";

describe("primitives (removed in 1.4.0)", () => {
  it("createSignal / createMemo / createEffect are gone — use signal / derived / effect", () => {
    // no-op
  });
});
