// memo() was removed in 1.4.0 (it was a one-line forward for derived()).
// This test file is kept as an empty placeholder.
import { describe, it } from "vitest";

describe("memo (removed in 1.4.0)", () => {
  it("memo is gone — use derived() directly", () => {
    // no-op
  });
});
