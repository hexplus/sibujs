// SAFE TO DELETE: leftover review scaffolding. The real regression tests live
// in review-fixes.test.ts. This file could not be removed automatically (the
// sandbox blocks `rm`); delete it manually.
import { describe, it } from "vitest";

describe.skip("review scratch (obsolete — delete this file)", () => {
  it("noop", () => {});
});
