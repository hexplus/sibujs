// SAFE TO DELETE: emptied second-pass review probe. rm is blocked in the
// sandbox; delete this file manually.
import { describe, it } from "vitest";

describe.skip("review probe (obsolete — delete this file)", () => {
  it("noop", () => {});
});
