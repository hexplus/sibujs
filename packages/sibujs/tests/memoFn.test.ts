// memoFn() was removed in 1.4.0 (it was a one-line forward for derived()).
// This test file is kept as an empty placeholder.
import { describe, it } from "vitest";

describe("memoFn (removed in 1.4.0)", () => {
  it("memoFn is gone — use derived() directly", () => {
    // no-op
  });
});
